export const environment = {
  production: true,
  apiUrl: '/api',
  // The transactional LEAN system the login tiles point at. Not the portal's
  // own address, so it stays on the ministry's domain wherever this is hosted.
  leanAppOrigin: 'https://lean.msme.gov.in',
  siteName: 'MSME Competitive (LEAN) Scheme',
};
